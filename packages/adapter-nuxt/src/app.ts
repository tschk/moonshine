/**
 * Nuxt app composables re-export (`#app` / `nuxt/app`).
 *
 * ```ts
 * import { useFetch, navigateTo, useRoute } from "@tschk/moonshine-nuxt/app";
 * ```
 */
export {
  navigateTo,
  useAsyncData,
  useCookie,
  useFetch,
  useRequestHeaders,
  useRoute,
  useRouter,
  useRuntimeConfig,
  useState,
} from "#app";
