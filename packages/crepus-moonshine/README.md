# `@tschk/crepus-moonshine`

Crepuscularity View IR → React (`renderCrepusIr`). Re-exports a light `@tschk/moonshine` surface for CLI emit.

In this monorepo the dependency is `"@tschk/moonshine": "workspace:*"`. Out-of-tree, point it at a published version or `file:../core`.
