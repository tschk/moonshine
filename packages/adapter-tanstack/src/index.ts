"use client";

/**
 * @tschk/moonshine-tanstack
 *
 * TanStack Start / Router client surface. Use moonshine signals in client
 * components; leave server functions / loaders on the server.
 * Host owns routing — MoonshineRouter is not re-exported.
 */

export * from "@tschk/moonshine/host-react";
