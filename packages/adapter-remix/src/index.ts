"use client";

/**
 * @tschk/moonshine-remix
 *
 * Remix / React Router v7 client surface. Prefer moonshine signals inside
 * browser route modules. Keep loaders/actions free of client hooks.
 * Host owns routing — MoonshineRouter is not re-exported.
 */

export * from "@tschk/moonshine/host-react";
