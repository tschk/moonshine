/**
 * next/headers re-export (Server Components / Route Handlers only).
 *
 * ```ts
 * import { headers, cookies } from "@tschk/moonshine-next/headers";
 * ```
 */
export { cookies, draftMode, headers } from "next/headers";
