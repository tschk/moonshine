"use client";

/**
 * @tschk/moonshine-next
 *
 * Next.js App Router client surface. Import only from Client Components.
 * Does **not** re-export MoonshineRouter — use the App Router.
 *
 * Server helpers: `@tschk/moonshine-next/server` or `@tschk/moonshine/server`.
 *
 * ```tsx
 * "use client";
 * import { createSignal, useSignal } from "@tschk/moonshine-next";
 * ```
 */

export * from "@tschk/moonshine/host-react";
