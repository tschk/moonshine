"use client";

/**
 * @tschk/moonshine-next
 *
 * Default export = **client** surface. For Route Handlers:
 *   import { moonshineRoute, moonshineJson } from "@tschk/moonshine-next/server"
 *
 * Does not export MoonshineRouter — use the App Router.
 */
export * from "./client";
