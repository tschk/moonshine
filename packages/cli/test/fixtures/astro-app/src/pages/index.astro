---
const title = "astro on moonshine";
const items = ["one", "two"];
const ok = true;
---

<main class="home">
  <h1>{title}</h1>
  <ul>{items.map(item => <li>{item}</li>)}</ul>
  {ok && <p class="note">frontmatter is not executed</p>}
</main>
