# `@tschk/moonshine-astro`

Re-exports moonshine for **Astro client islands**. Use with React islands (`client:load`, etc.).

```tsx
"use client";
import { createSignal, useSignal } from "@tschk/moonshine-astro";
```
