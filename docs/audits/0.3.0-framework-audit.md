# 0.3.0 Framework Audit

> 2026-07-31: historical record. `scripts/benchmark.ts` and
> `scripts/benchmark-competitive.ts` were removed as internal-only tooling in
> 0.3.2; the run commands below are not current procedures.

Environment: macOS arm64, Bun 1.3.14, TypeScript 7.0.2, Dart 3.12.2.

## Step 1 — Correctness and impact analysis

| Area                       | Method                                                                                                                                                                                                         | Finding                                                                                                                                               | Verdict                                                                                                                                                  |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Route precedence           | Reviewed `packages/router/src/pattern.ts` and `packages/router/src/graph.ts`; ran `packages/router/test/graph.test.ts`.                                                                                        | Static (3) beats dynamic (2) beats optional (1) beats rest (0). `compareMatches` correctly treats missing segments as lower precedence.               | Pass.                                                                                                                                                    |
| Request cancellation       | Reviewed `packages/server/src/pipeline.ts` and `packages/deploy-node/src/index.ts`; ran `packages/adapter-conformance/src/suite.ts` cancellation test.                                                         | `ctx.signal.throwIfAborted()` runs before/after loaders and before renderer. Node bridge forwards abort on `close` and removes listeners.             | Pass.                                                                                                                                                    |
| Serialization              | Reviewed `packages/server/src/serialize.ts` and `packages/react/src/serialize.ts`; ran `packages/server/test/serialize.test.ts` and `packages/react/test/islands.test.tsx`.                                    | `serializeData` escapes `<`, `>`, `&`, U+2028, U+2029 and rejects functions, symbols, and cycles. `serializeIslandProps` escapes HTML-sensitive JSON. | Pass.                                                                                                                                                    |
| Static traversal guard     | Reviewed `packages/server/src/static.ts` and all callers.                                                                                                                                                      | `resolveStaticPath` decoded the entire path before validation and threw on malformed percent-encoding (`/%` => URI malformed).                        | Fixed: per-segment decode, reject `.`, `..`, null, separators, and malformed percent. Call-site `!pathname.includes("..")` filters removed as redundant. |
| Cache isolation            | Reviewed `packages/deploy-cloudflare/src/index.ts` and `packages/server/src/pipeline.ts`.                                                                                                                      | Cloudflare `caches.default` used only when a route declares a cache policy; request state is local to each handler.                                   | Pass.                                                                                                                                                    |
| Adapter capability failure | Reviewed `packages/deploy-cloudflare/src/index.ts` and `packages/deploy-vercel/src/index.ts`; ran `packages/deploy-cloudflare/test/conformance.test.ts` and `packages/deploy-vercel/test/conformance.test.ts`. | Both adapters validate runtimes and capabilities and throw descriptive errors.                                                                        | Pass.                                                                                                                                                    |
| Compatibility exports      | Reviewed `packages/core/src/router.ts`, `packages/core/src/server.ts`, `packages/core/src/react.ts`; ran `packages/core/test/adapters-contract.test.ts`.                                                       | Core re-exports `router`, `server`, and `react` from the new packages and keeps host-library subpaths.                                                | Pass.                                                                                                                                                    |

## Step 2 — Security checks

| Check                        | Command                                                                                                                 | Result                                                                                                                                                                                     |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Secret scan                  | `rg -i 'password\|secret\|token\|api_key\|-----BEGIN\|AWS_\|PRIVATE' --glob '*.{ts,tsx,js,jsx,json,md,toml,env,txt}' .` | No real secrets found; only `private` package fields, a test error string, and documentation.                                                                                              |
| Dependency trust             | `bun pm untrusted`                                                                                                      | 0 untrusted dependencies with scripts. Bun security scanner is not configured (`bun pm scan` reports no scanner). All installed packages are workspace or well-known runtime deps.         |
| Static analysis              | `bunx oxlint .`                                                                                                         | 0 errors, 2 intentional warnings in Crepus IR test fixtures (`then` property is part of the IR schema, not a Promise thenable).                                                            |
| Encoded traversal            | Added `packages/server/test/security.test.ts`                                                                           | `resolveStaticPath` blocks `%2e%2e`, `%2f..%2f`, double-encoded forms, embedded null, and malformed percent.                                                                               |
| Malformed URI parameters     | Ran `packages/router/test/pattern.test.ts` and new security tests.                                                      | `compilePattern` and `decodePart` return `null` for malformed encoding instead of crashing.                                                                                                |
| HTML/script injection        | `packages/server/test/serialize.test.ts` and `packages/react/test/islands.test.tsx`                                     | Output contains `\u003c/script\u003e` escapes, no literal `<`, `>`, or `&`, and U+2028/2029 are escaped.                                                                                   |
| Open redirects               | Added `packages/server/test/security.test.ts`                                                                           | `redirect` now enforces same-origin or relative paths; `//evil.com` and CR/LF location attempts return 400.                                                                                |
| Header injection             | Added `packages/deploy-node/test/security.test.ts`                                                                      | `createNodeHandler` removes `connection`, `content-length`, `transfer-encoding`, and other hop-by-hop/framing headers from both request and response. `Headers.set` already rejects CR/LF. |
| Request smuggling boundaries | Manual review + Node bridge header filter test                                                                          | Node parser rejects conflicting `Content-Length` + `Transfer-Encoding` as 400. Bridge no longer forwards framing headers, removing desync surface.                                         |
| Cross-request state leakage  | Added concurrent request test in `packages/server/test/security.test.ts`                                                | Concurrent `/hello/:name` requests produce isolated data (`alice` / `bob`). Pipeline `ctx.data` is per request.                                                                            |

## Step 3 — Benchmark results

Run: `bun scripts/benchmark.ts --out docs/audits/benchmark-results.md`.

```
- bun: 1.3.14
- os: darwin arm64
- runs per measurement: 10
- counts: 10, 100, 1000

| routes | discovery (ms) | classification (ms) | manifest (ms) | cold build (ms) | incremental build (ms) | server startup (ms) | static request (ms) | SSR request (ms) |
|-------:|---------------:|--------------------:|--------------:|----------------:|-----------------------:|--------------------:|--------------------:|-----------------:|
| 10 | 0.53 | 4.01 | 2.75 | 19.14 | 12.12 | 2.27 | 1.11 | 1.20 |
| 100 | 3.01 | 8.06 | 13.03 | 31.50 | 27.49 | 0.15 | 0.54 | 0.56 |
| 1000 | 20.24 | 77.11 | 98.59 | 185.59 | 182.40 | 3.61 | 0.98 | 2.58 |
```

The full markdown table is also at `docs/audits/benchmark-results.md`.

## Step 4 — Complexity audit

| Item                          | Finding                                                                                                                                                                                                                                                             | Action                                                                                                                                                                                 |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Duplicate traversal prefilter | `!pathname.includes("..")` appeared in `packages/server/src/pipeline.ts`, `packages/core/src/server.ts`, `packages/deploy-bun/src/index.ts`, `packages/deploy-node/src/index.ts`, `packages/deploy-cloudflare/src/index.ts`, `packages/deploy-vercel/src/index.ts`. | Removed from all call sites; the shared `resolveStaticPath` now handles traversal.                                                                                                     |
| Duplicate `toPosix` helpers   | `toPosix` existed in `packages/compiler/src/build.ts`, `discover.ts`, `manifest.ts`, `inherit.ts`, and `scripts/benchmark.ts`.                                                                                                                                      | Created `packages/compiler/src/path.ts` and deleted the four compiler duplicates. Benchmark imports the shared helper.                                                                 |
| Lint hygiene                  | Oxlint flagged one unused variable, one `new Array(singleArgument)`, and one unnecessary `[...set]` spread.                                                                                                                                                         | Fixed in `packages/core/test/adapters-contract.test.ts`, `components/src/dither/dither-paint.ts`, and `packages/core/src/signal.ts`. Crepus `then` warnings are intentional IR schema. |
| One-use abstractions          | `routeToRegex` and `staticSlug` in `packages/deploy-vercel/src/index.ts` are only used by the Vercel adapter. `hashString` in `packages/react/src/islands.tsx` is only used for island IDs.                                                                         | No deletion warranted; they are adapter/renderer specific and small.                                                                                                                   |
| Duplicate adapter helpers     | `resolveManifestFiles`, `copyAssets`, `validateRuntime`, `validateCapabilities` are duplicated between `deploy-cloudflare` and `deploy-vercel`. `copyAsset` is duplicated between `deploy-bun` and `deploy-node` (different Node/Bun implementations).              | No deletion warranted; the code is adapter-specific and not public API. Recorded for future sharing if a deploy-shared package is added.                                               |
| C++ native code               | None present.                                                                                                                                                                                                                                                       | N/A; no C++ added. No native policy gates triggered.                                                                                                                                   |

## Step 5 — Findings and resolutions

| ID      | Severity | Finding                                                                                                         | Resolution                                                                                                                                                                                  | Commit      |
| ------- | -------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| AUD-001 | High     | `resolveStaticPath` crashed on malformed percent-encoding (`/%`) and decoded the entire path before validation. | Hardened `resolveStaticPath` with per-segment decode and rejection of traversal patterns; added `packages/server/test/security.test.ts`.                                                    | This commit |
| AUD-002 | High     | Open redirects and header injection through `redirect` location were possible.                                  | `createRequestHandler` now validates redirect location with `new URL(location, request.url).origin === base origin` and returns 400 for external or malformed values; added security tests. | This commit |
| AUD-003 | Medium   | Node bridge forwarded hop-by-hop/framing headers (`Content-Length`, `Transfer-Encoding`, `Connection`).         | Added header filtering in `packages/deploy-node/src/index.ts` for both request and response; added `packages/deploy-node/test/security.test.ts`.                                            | This commit |
| AUD-004 | Low      | Frozen lockfile gate failed on initial run due to missing `@tschk/moonshine-shaders` workspace entry.           | Ran `bun install` and saved the updated `bun.lock`.                                                                                                                                         | This commit |
| AUD-005 | Low      | Duplicate `toPosix` helpers and redundant traversal prefilters.                                                 | Created `packages/compiler/src/path.ts`; removed duplicate helpers and call-site prefilters.                                                                                                | This commit |
| AUD-006 | Low      | Oxlint warnings for unused vars, `new Array(singleArgument)`, and `[...set]` spread.                            | Renamed/removed/converted to `Array.from`.                                                                                                                                                  | This commit |

## Step 6 — Release verdict

All gates were re-run:

```bash
bun install --frozen-lockfile
bun run format
bun run lint
bun run typecheck
bun run typecheck:native
bun test
bun run check
bun run check:size
bun run test:packed
(cd dart/moonshine_jaspr && dart analyze && dart test)
```

Results: all commands exited 0. No unresolved high/critical security finding, failing conformance target, or unverified capability claim remains.

**Verdict: Release 0.3.0 is approved.**

## Step 7 — Version and release checks

### Pre-version release check

Command: `bun scripts/release.ts --check 0.3.0`

Result: failed at the package version check. Output:

```
packages still at 0.2.0 (expected 0.3.0):
  @tschk/crepus-moonshine
  @tschk/moonshine
  @tschk/moonshine-adapter-conformance
  @tschk/moonshine-angular
  @tschk/moonshine-astro
  @tschk/moonshine-cli
  @tschk/moonshine-compiler
  @tschk/moonshine-components
  @tschk/moonshine-deploy-bun
  @tschk/moonshine-deploy-cloudflare
  @tschk/moonshine-deploy-node
  @tschk/moonshine-deploy-vercel
  @tschk/moonshine-framework
  @tschk/moonshine-next
  @tschk/moonshine-nuxt
  @tschk/moonshine-react
  @tschk/moonshine-remix
  @tschk/moonshine-router
  @tschk/moonshine-server
  @tschk/moonshine-shaders
  @tschk/moonshine-solid
  @tschk/moonshine-svelte
  @tschk/moonshine-tanstack
  @tschk/moonshine-vue
  @tschk/moonshine-waku
```

All public packages and `components/package.json` were still at version `0.2.0` before versioning. Dependency ranges still used `^0.2.0`, `workspace:*`, and `file:` references.

### Post-version release check

Command: `bun scripts/release.ts --check 0.3.0`

Result: passed with one npm auth/ownership warning. Output:

```
release check passed with 1 npm auth/ownership warning(s)
warning: npm identity not available: npm error code E401
npm error 401 Unauthorized - GET https://registry.npmjs.org/-/whoami
```

All local checks passed: git status, branch tracking, package versions, internal dependency ranges, and packed manifest integrity. The npm identity/ownership check is not configured in this environment and is recorded as an expected environment limitation.
